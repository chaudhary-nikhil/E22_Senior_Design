# Nanopb options file for imu_sample.proto
# These options optimize the generated code for embedded use

# Set maximum count for repeated samples field (batch size)
formsync.ImuSampleBatch.samples  max_count:256
